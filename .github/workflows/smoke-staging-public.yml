name: Smoke staging public
run-name: Smoke staging public

on:
  workflow_dispatch:
    inputs:
      public_base_url:
        description: Optional HTTPS staging URL. Defaults to STAGING_PUBLIC_BASE_URL.
        required: false
        type: string

permissions:
  contents: read

concurrency:
  group: staging-public-smoke
  cancel-in-progress: false

env:
  STAGING_PUBLIC_BASE_URL: ${{ vars.STAGING_PUBLIC_BASE_URL }}

jobs:
  smoke:
    name: Public smoke
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    environment: staging

    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Verify public staging
        shell: bash
        env:
          INPUT_PUBLIC_BASE_URL: ${{ inputs.public_base_url }}
        run: |
          set -euo pipefail

          base_url="${INPUT_PUBLIC_BASE_URL:-${STAGING_PUBLIC_BASE_URL:-}}"
          ./scripts/verify-staging-public-smoke.sh --base-url "$base_url"

          {
            echo "## Staging public smoke"
            echo
            echo "- Base URL: \`${base_url%/}\`"
            echo "- Web root: passed"
            echo "- Public timeline: passed"
          } >> "$GITHUB_STEP_SUMMARY"
