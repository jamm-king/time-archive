# Production R2 Readiness

## Purpose

This runbook defines the production Cloudflare R2 readiness requirements for
Time Archive. It covers bucket isolation, credentials, CORS, private access,
object-reference policy, and verification before paid production launch.

No production R2 resource is created by this document.

## Required Production Resources

Production requires:

- a dedicated production R2 bucket;
- production-only R2 access key ID and secret access key;
- least-privilege access limited to the production bucket;
- bucket CORS scoped to the production Web origin;
- no public read policy for original uploads;
- production SSM parameters under `/time-archive/production/r2/`;
- documented verification of upload, admin preview, and public playback.

Do not reuse local or staging buckets, access keys, custom domains, or object
prefixes in production.

## Bucket Policy

Original uploaded media remains private. Public timeline playback must use
short-lived presigned URLs generated by the API from approved database object
references.

Do not attach a public custom domain or public-read policy to the original
upload bucket if that domain would let users bypass moderation or presigned
playback policy.

If future CDN-served derived media is introduced, use a separate derived-media
bucket or prefix with its own publication policy.

## Runtime Configuration

Production R2 parameters map to these environment variables:

```text
TIME_ARCHIVE_STORAGE_S3_ENDPOINT
TIME_ARCHIVE_STORAGE_S3_PRESIGNED_URL_ENDPOINT
TIME_ARCHIVE_STORAGE_S3_PUBLIC_BASE_URL
TIME_ARCHIVE_STORAGE_S3_BUCKET
TIME_ARCHIVE_STORAGE_S3_REGION=auto
TIME_ARCHIVE_STORAGE_S3_PATH_STYLE_ACCESS=true
TIME_ARCHIVE_STORAGE_S3_ACCESS_KEY
TIME_ARCHIVE_STORAGE_S3_SECRET_KEY
```

`TIME_ARCHIVE_STORAGE_S3_PUBLIC_BASE_URL` is a managed object-reference base
stored in database rows. Despite the property name, it must not imply public
anonymous access to original objects.

## CORS

Production R2 CORS must allow only the deployed production Web origin.

Minimum browser-upload policy:

```json
[
  {
    "AllowedOrigins": [
      "https://time-archive.com"
    ],
    "AllowedMethods": [
      "PUT",
      "GET",
      "HEAD"
    ],
    "AllowedHeaders": [
      "Content-Type",
      "Content-Length",
      "x-amz-*"
    ],
    "ExposeHeaders": [
      "ETag"
    ],
    "MaxAgeSeconds": 3000
  }
]
```

Adjust the hostname only if the approved production application hostname is
different. Do not use wildcard origins for authenticated upload flows.

## Verification

Before paid production launch, verify:

1. Production bucket exists and differs from local and staging buckets.
2. Access key can read, write, and head only the production bucket.
3. Bucket is not publicly listable or readable.
4. Presigned PUT succeeds through the production Web origin.
5. Presigned admin preview GET succeeds for an authorized admin.
6. Public timeline returns presigned playback URLs only for approved media.
7. Hidden and rejected media remain absent from the timeline.
8. Cloudflare shared cache does not cache API responses carrying presigned URLs.
9. SSM metadata shows R2 credentials as `SecureString`.
10. No production logs contain R2 credentials or presigned URLs.

## Rollback

If production R2 verification fails before launch, do not approve production
media uploads. Fix CORS, credentials, or runtime parameters and redeploy.

If production R2 fails after launch:

1. Stop approving new media if playback or preview is unreliable.
2. Preserve request IDs, media asset IDs, object keys, and deployment SHA.
3. Determine whether the failure is credentials, CORS, bucket policy, object
   absence, or application runtime configuration.
4. Avoid changing bucket or object-reference base URL unless following
   [Storage Backend Change Procedure](storage-backend-change-procedure.md).

## Release Gate

Production Cloudflare R2 can be marked ready only after production bucket,
credential, CORS, upload, preview, and playback verification all pass against
the production deployment.
